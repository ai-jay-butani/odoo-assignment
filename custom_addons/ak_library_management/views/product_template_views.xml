<odoo>
    <!--inherit product.template form view-->
    <record id="product_template_form_view_inherit_lms" model="ir.ui.view">
        <field name="name">product.template.form.view</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="mark_as_borrowed" string="Mark as Borrowed" type="object"
                invisible="status != 'available'"/>
                <button name="mark_as_available" string="Mark as Available" type="object"
                invisible="status != 'borrowed'"/>
                <field name="status" widget="statusbar" statusbar_visible="available,borrowed,returned"
                       options="{'clickable':'1'}"/>
                <button name="borrowed_books" string="Borrowed Books" type="object"/>
            </xpath>
            <xpath expr="//field[@name='standard_price']" position="attributes">
                <attribute name="readonly">sale_ok</attribute>
            </xpath>
            <xpath expr="//page[@name='inventory']" position="after">
                <page name="book details" string="Book Details">
                    <group name="book details">
                       <group>
                             <field name="is_library_book"/>
                             <field name="author" required="sale_ok"/>
                             <field name="publisher" required="sale_ok"/>
                       </group>
                        <group>
                            <field name="edition" required="sale_ok"/>
                            <field name="published_date" required="sale_ok"/>
                            <field name="pages"/>
                            <field name="available"/>
                            <field name="status"/>
                       </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <!--inherit menu-->
    <menuitem id="product_template_books_menu" name="Products"
              parent="menu_library" action="product.product_template_action"/>
</odoo>
