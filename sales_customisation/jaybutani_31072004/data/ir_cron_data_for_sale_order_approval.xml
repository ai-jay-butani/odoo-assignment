<odoo>
    <!--pending sale order approval remainder schedule action-->
    <record id="ir_cron_pending_sale_order_approval" model="ir.cron">
        <field name="name">Pending sale order approval remainder</field>
        <field name="model_id" ref="model_sale_order"/>
        <field name="state">code</field>
        <field name="code">model.action_reminder_sale_order_approval()</field>
        <field name='interval_number'>1</field>
        <field name='interval_type'>days</field>
    </record>
</odoo>